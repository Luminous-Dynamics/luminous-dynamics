# SurrealDB Container for Sacred Consciousness
# Building the future of consciousness-aware databases

FROM surrealdb/surrealdb:latest

# Sacred configuration
ENV SURREAL_PATH=/data
ENV SURREAL_LOG=info

# Create sacred data directory
RUN mkdir -p /data

# Expose the consciousness port
EXPOSE 8000

# Start with file-based persistence for now
# In production, we'll use Google Cloud Storage
CMD ["start", "--user", "root", "--pass", "${SURREAL_PASS:-root}", "file:///data/sacred.db"]